# เงื่อนไขการอ่านและดึงข้อมูล Invoice

## 1. เงื่อนไขการตรวจสอบเอกสาร MST

### ต้องมีทั้ง 2 เงื่อนไข:
1. **ชื่อบริษัท**: `"Mediterranean Shipping (Thailand) Co., Ltd."` ต้องมีในเอกสาร
2. **Tax ID**: `"0105544019079"` ต้องมีในเอกสาร

```python
has_company = "Mediterranean Shipping (Thailand) Co., Ltd." in text
has_tax_id = "0105544019079" in text
return has_company and has_tax_id
```

## 2. เงื่อนไขการดึงยอดเงิน (MST)

### 2.1 การทำความสะอาดข้อความ
1. ลบ newline ที่แทรกอยู่ในคำ: `(\S)\s*\n\s*(\S)` → `\1\2`
2. ลบ space หลายตัวในคำไทย: `([ก-๙])\s+([ก-๙])` → `\1\2`
3. เพิ่ม space ระหว่างตัวเลขกับตัวอักษร: `([\d,]+\.?\d*)([A-Za-zก-๙])` → `\1 \2`

### 2.2 Pattern สำหรับยอดก่อนภาษี (บรรทัด 1 - มีภาษี)

**รูปแบบที่รองรับ:**
1. `1,800.00Taxable Amount | / | ก่อนภาษีมูลค่าเพิ่ม | 1,800.00` (ตัวเลขติดกับคำ)
2. `Taxable Amount | / | ก่อนภาษีมูลค่าเพิ่ม | 1,800.00`
3. `ก่อนภาษีมูลค่าเพิ่ม | 1,800.00`
4. `ก่อนภาษีมูลค่าเพิ่ม : 1,800.00`
5. `ก่อนภาษีมูลค่าเพิ่ม 1,800.00`
6. `Taxable Amount | 1,800.00` (ภาษาอังกฤษ)

**Patterns:**
```python
patterns_before_vat = [
    r'[\d,]+\.?\d*\s*Taxable\s+Amount[:\s|/]*ก่อนภาษีมูลค่าเพิ่ม[:\s|]*([\d,]+\.?\d*)',
    r'Taxable\s+Amount[:\s|/]*ก่อนภาษีมูลค่าเพิ่ม[:\s|]*([\d,]+\.?\d*)',
    r'ก่อนภาษีมูลค่าเพิ่ม\s*\|\s*([\d,]+\.?\d*)',
    r'ก่อนภาษีมูลค่าเพิ่ม\s*[:|]\s*([\d,]+\.?\d*)',
    r'ก่อนภาษีมูลค่าเพิ่ม\s+([\d,]+\.?\d*)',
    r'ก่อนภาษีมูลค่าเพิ่ม[:\s]*([\d,]+\.?\d*)',
    r'Taxable\s+Amount[:\s|]*([\d,]+\.?\d*)',
    r'[\d,]+\.?\d*\s*Taxable\s+Amount[:\s|]*([\d,]+\.?\d*)',
]
```

**Fallback Pattern:**
```python
r'ก่อนภาษี[^|]*\|[^|]*\|[^|]*\|[^|]*([\d,]+\.?\d*)'
```

### 2.3 Pattern สำหรับยอดก่อนภาษี (บรรทัด 2 - ไม่มีภาษี)

**รูปแบบที่รองรับ:**
1. `Non-Taxable Amount | / | ไม่มีภาษีมูลค่าเพิ่ม | 1,500.00`
2. `ไม่มีภาษีมูลค่าเพิ่ม | 1,500.00`
3. `ไม่มีภาษีมูลค่าเพิ่ม : 1,500.00`
4. `ไม่มีภาษีมูลค่าเพิ่ม 1,500.00`
5. `Non-Taxable Amount | 1,500.00` (ภาษาอังกฤษ)

**Patterns:**
```python
patterns_non_vat = [
    r'Non-Taxable\s+Amount[:\s|/]*ไม่มีภาษีมูลค่าเพิ่ม[:\s|]*([\d,]+\.?\d*)',
    r'ไม่มีภาษีมูลค่าเพิ่ม\s*\|\s*([\d,]+\.?\d*)',
    r'ไม่มีภาษีมูลค่าเพิ่ม\s*[:|]\s*([\d,]+\.?\d*)',
    r'ไม่มีภาษีมูลค่าเพิ่ม\s+([\d,]+\.?\d*)',
    r'ไม่มีภาษีมูลค่าเพิ่ม[:\s]*([\d,]+\.?\d*)',
    r'Non-Taxable\s+Amount[:\s|]*([\d,]+\.?\d*)',
]
```

**Fallback Pattern:**
```python
r'ไม่มีภาษี[^|]*\|[^|]*\|[^|]*\|[^|]*([\d,]+\.?\d*)'
```

### 2.4 Pattern สำหรับยอดภาษี

**รูปแบบที่รองรับ:**
1. `7% VAT | / | ภาษีมูลค่าเพิ่ม | 126.00`
2. `ภาษีมูลค่าเพิ่ม | 126.00` (ต้องไม่มีคำว่า "ก่อน" หรือ "ไม่มี" ข้างหน้า)
3. `7% VAT | 126.00`
4. `VAT | 126.00`

**Patterns:**
```python
patterns_vat = [
    r'(?:7%|7\s*%)\s*VAT[:\s|/]*ภาษีมูลค่าเพิ่ม[:\s|]*([\d,]+\.?\d*)',
    r'(?<!ก่อน)(?<!ไม่มี)ภาษีมูลค่าเพิ่ม\s*\|\s*([\d,]+\.?\d*)',
    r'(?<!ก่อน)(?<!ไม่มี)ภาษีมูลค่าเพิ่ม\s*[:|]\s*([\d,]+\.?\d*)',
    r'(?<!ก่อน)(?<!ไม่มี)ภาษีมูลค่าเพิ่ม\s+([\d,]+\.?\d*)',
    r'(?<!ก่อน)(?<!ไม่มี)ภาษีมูลค่าเพิ่ม[:\s]*([\d,]+\.?\d*)',
    r'(?:7%|7\s*%)\s*VAT[:\s|]*([\d,]+\.?\d*)',
    r'VAT[:\s|]*([\d,]+\.?\d*)',
]
```

### 2.5 Pattern สำหรับยอดรวม

**รูปแบบที่รองรับ:**
1. `Total | / | รวม | 3,426.00`
2. `รวม | 3,426.00`
3. `รวม : 3,426.00`
4. `รวม 3,426.00`
5. `Total | 3,426.00` (ภาษาอังกฤษ)

**Patterns:**
```python
patterns_total = [
    r'Total[:\s|/]*รวม[:\s|]*([\d,]+\.?\d*)',
    r'รวม\s*\|\s*([\d,]+\.?\d*)',
    r'รวม\s*[:|]\s*([\d,]+\.?\d*)',
    r'รวม\s+([\d,]+\.?\d*)',
    r'รวม[:\s]*([\d,]+\.?\d*)',
    r'Total[:\s|]*([\d,]+\.?\d*)',
]
```

## 3. ตัวอย่างข้อความจาก PDF

```
2 | CLEANING | 1,800.00Taxable Amount | / | ก่อนภาษีมูลค่าเพิ่ม | 1,800.00
Non-Taxable Amount | / | ไม่มีภาษีมูลค่าเพิ่ม | 1,500.00
7% VAT | / | ภาษีมูลค่าเพิ่ม | 126.00
Total | / | รวม | 3,426.00
```

## 4. Debug Logging

ระบบจะ log ข้อมูลต่อไปนี้:
- `🔍 MST extract_amounts - Text length: {len(text)}`
- `🔍 MST extract_amounts - First 500 chars: {text[:500]}`
- `🔍 Text after cleaning (first 1000 chars): {text_clean[:1000]}`
- `🔍 Pattern {idx+1} matched: '{pattern}' -> '{match.group(0)}' -> amount: '{amount_str}'`
- `🔍 Pattern {idx+1} ไม่ match: '{pattern}'`
- `✅ พบยอดก่อนภาษี (บรรทัด 1): {amount} (Pattern {idx+1})`
- `⚠️ ไม่พบยอดก่อนภาษี (บรรทัด 1) - ลองค้นหาด้วยวิธีอื่น...`
- `📊 MST extract_amounts Results:`

## 5. การแสดงผลใน Terminal

เมื่ออ่าน PDF เสร็จ ระบบจะแสดง:
```
💰 ยอดเงินที่อ่านได้ (MST):
   🔹 MST: บันทึก 2 แถว
      📊 แถวที่ 1 (มีภาษี):
         • ยอดก่อนภาษี: 1,800.00 บาท
         • ยอดภาษี: 126.00 บาท
         • ยอดรวม: 1,926.00 บาท
      📊 แถวที่ 2 (ไม่มีภาษี):
         • ยอดก่อนภาษี: 1,500.00 บาท
         • ยอดภาษี: 0.00 บาท
         • ยอดรวม: 1,500.00 บาท
      💵 ยอดรวมทั้ง 2 แถว: 3,426.00 บาท
```

