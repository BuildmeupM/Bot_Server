# 📋 รายละเอียดระบบแยกประเภทบริษัทและข้อมูลที่อ่านได้

## 🏢 บริษัทที่รองรับ

ระบบรองรับการอ่านข้อมูลจากเอกสารของ **2 บริษัท**:

1. **MSC** - MSC Mediterranean Shipping Company S.A.
2. **MST** - Mediterranean Shipping (Thailand) Co., Ltd.

---

## 🔍 วิธีตรวจสอบประเภทบริษัท

### MSC (MSC Mediterranean Shipping Company)
**เงื่อนไข:**
- ต้องมีข้อความ: `"MSC Mediterranean Shipping Company"` หรือ `"Mediterranean Shipping (Thailand)"`

### MST (Mediterranean Shipping Thailand)
**เงื่อนไข (ต้องมีทั้ง 2 ข้อ):**
1. ต้องมีข้อความ: `"Mediterranean Shipping (Thailand) Co., Ltd."`
2. ต้องมี Tax ID: `"0105544019079"`

> **หมายเหตุ:** MST Extractor จะถูกตรวจสอบก่อน MSC เพราะเฉพาะเจาะจงกว่า

---

## 📊 ข้อมูลที่อ่านได้จากแต่ละบริษัท

### ✅ ข้อมูลที่อ่านได้เหมือนกันทั้ง 2 บริษัท

| ข้อมูล | วิธีอ่าน | ตัวอย่าง |
|------|---------|---------|
| **ชื่อบริษัท** | หาจากข้อความในเอกสาร | `MSC Mediterranean Shipping Company S.A.` |
| **เลขประจำตัวผู้เสียภาษี** | Pattern: `TaxID` หรือ `Tax ID No.` + เลข 13 หลัก | `0993000003667`, `0105544019079` |
| **วันที่** | Pattern: `Date / วันที่ DD-MMM-YYYY` | `03/11/2025` |
| **ที่อยู่** | หาจาก "Head Office:", "Address:", "ที่อยู่:" | `571 Sukhumvit 71 Rd., Klongton-Nua, Vadhana, Bangkok 10110` |
| **ชื่อบัญชี / โค้ดบัญชี** | ยังไม่อ่านจากเอกสาร (ต้องดึงจาก Chart of Accounts) | `None` |
| **เปอร์เซ็นต์หัก ณ ที่จ่าย** | Pattern: `หัก ณ ที่จ่าย` หรือ `Withholding Tax` + % | `3%`, `0%` |
| **ยอดก่อนภาษีมูลค่าเพิ่ม** | แตกต่างกันตามบริษัท (ดูด้านล่าง) | |
| **ยอดภาษีมูลค่าเพิ่ม** | แตกต่างกันตามบริษัท (ดูด้านล่าง) | |
| **ยอดรวม** | Pattern: `Total / รวม` | `6,000.00` |
| **ประเภทเอกสาร** | ตรวจจับจากยอดภาษี | `1` = มีภาษี, `2` = ไม่มีภาษี |

---

## 📝 ข้อมูลเฉพาะของแต่ละบริษัท

### 1️⃣ MSC (MSC Mediterranean Shipping Company)

#### ข้อมูลที่อ่านได้เพิ่มเติม:
| ข้อมูล | วิธีอ่าน | ตัวอย่าง |
|------|---------|---------|
| **เลขที่ใบแจ้งหนี้** | Pattern: `No.` + เลข 10 หลักขึ้นไป | `2511200301` |
| **เลข BL (Bill of Lading)** | Pattern: `BL(s) :` หรือ `BL:` + รหัส | `MEDUW0265381` |
| **เลขที่เอกสาร** | ดึงจากชื่อไฟล์ (ส่วนแรกก่อน `_`) | `EXC-2511-008` |
| **ยอดก่อนภาษี** | Pattern: `Non-Taxable Amount / ไม่มีภาษีมูลค่าเพิ่ม` | `6,000.00` |
| **หมายเหตุ** | `BL Number + Document Number` หรือ `Document Number` อย่างเดียว | `MEDUW0265381 EXC-2511-008` |
| **ชื่อไฟล์ใหม่** | `Invoice Number.pdf` | `2511200301.pdf` |

#### ข้อมูลที่ส่งออก:
```python
{
    'success': True,
    'company': 'MSC',
    'company_name': 'MSC Mediterranean Shipping Company S.A.',
    'tax_id': '0993000003667',
    'date': '03/11/2025',
    'address': '571 Sukhumvit 71 Rd., Klongton-Nua, Vadhana, Bangkok 10110',
    'account_name': None,
    'account_code': None,
    'withholding_tax_percent': 0.0,
    'withholding_tax_amount': 0.0,
    'amount_before_vat': 6000.00,
    'vat_amount': 0.0,
    'total_amount': 6000.00,
    'remark': 'MEDUW0265381 EXC-2511-008',
    'new_filename': '2511200301.pdf',
    'old_filename': 'EXC-2511-008_007.pdf',
    'filepath': '/path/to/file.pdf',
    'document_type': 2,  # 2 = ไม่มีภาษี
    'bl_number': 'MEDUW0265381'
}
```

---

### 2️⃣ MST (Mediterranean Shipping Thailand)

#### ข้อมูลที่อ่านได้เพิ่มเติม:
| ข้อมูล | วิธีอ่าน | ตัวอย่าง |
|------|---------|---------|
| **เลขที่เอกสาร** | Pattern: `No.` + เลข | `2510104513` |
| **เลข BL (Bill of Lading)** | Pattern: `BL(s) :` หรือ `BL:` + รหัส | `MEDUW0265381` |
| **ยอดก่อนภาษี (บรรทัดที่ 1)** | Pattern: `ก่อนภาษีมูลค่าเพิ่ม` หรือ `Taxable Amount` | `1,800.00` |
| **ยอดก่อนภาษี (บรรทัดที่ 2)** | Pattern: `ไม่มีภาษีมูลค่าเพิ่ม` หรือ `Non-Taxable Amount` | `1,500.00` |
| **ยอดภาษี** | Pattern: `ภาษีมูลค่าเพิ่ม` หรือ `VAT` | `126.00` |
| **หมายเหตุ** | `BL Number + ชื่อไฟล์เก่า` หรือ `ชื่อไฟล์เก่า` อย่างเดียว | `MEDUW0265381 2510104513.pdf` |
| **ชื่อไฟล์ใหม่** | ใช้ชื่อเดิม | `2510104513.pdf` |

#### ข้อมูลพิเศษ:
- **MST จะอ่านยอดเงิน 2 บรรทัด:**
  - บรรทัดที่ 1: ยอดก่อนภาษี (มีภาษี) + ภาษี 7%
  - บรรทัดที่ 2: ยอดก่อนภาษี (ไม่มีภาษี)
- **เมื่อบันทึกลง Excel จะสร้าง 2 แถวในชีต "มีภาษีมูลค่าเพิ่ม"** (ใช้ลำดับเดียวกัน)

#### ข้อมูลที่ส่งออก:
```python
{
    'success': True,
    'company': 'MST',
    'company_name': 'Mediterranean Shipping (Thailand) Co., Ltd.',
    'tax_id': '0105544019079',
    'date': '15/10/2025',
    'address': 'Address from document',
    'document_number': '2510104513',
    'account_name': None,
    'account_code': None,
    'withholding_tax_percent': 0.0,
    'withholding_tax_amount': 0.0,
    'amount_before_vat': 3300.00,  # รวมทั้ง 2 บรรทัด (1,800 + 1,500)
    'vat_amount': 126.00,
    'total_amount': 3426.00,  # รวมทั้งหมด (3,300 + 126)
    'remark': 'MEDUW0265381 2510104513.pdf',
    'new_filename': '2510104513.pdf',
    'old_filename': '2510104513.pdf',
    'filepath': '/path/to/file.pdf',
    'document_type': 1,  # 1 = มีภาษี
    # ข้อมูลเพิ่มเติมสำหรับ MST
    'bl_number': 'MEDUW0265381',
    'amount_before_vat_line1': 1800.00,  # บรรทัดที่ 1 (มีภาษี)
    'amount_before_vat_line2': 1500.00   # บรรทัดที่ 2 (ไม่มีภาษี)
}
```

---

## 🔄 ลำดับการตรวจสอบ

ระบบจะตรวจสอบเอกสารตามลำดับนี้:

1. **MSTInvoiceExtractor** (ตรวจสอบก่อน - เพราะเฉพาะเจาะจงกว่า)
   - ตรวจสอบทั้งชื่อบริษัทและ Tax ID
2. **MSCInvoiceExtractor** (ตรวจสอบทีหลัง)
   - ตรวจสอบเฉพาะชื่อบริษัท

---

## 📌 หมายเหตุสำคัญ

### MST Special Handling
- **MST จะสร้าง 2 แถวใน Excel:**
  - แถวที่ 1: ยอดก่อนภาษี (บรรทัด 1) + ภาษี 7%
  - แถวที่ 2: ยอดก่อนภาษี (บรรทัด 2) + ภาษี 0%
- **ทั้ง 2 แถวใช้ลำดับเดียวกัน** (เพราะเป็นเอกสารเดียวกัน)

### Tax ID Handling (MSC)
- ถ้า Tax ID ขึ้นต้นด้วย `99` จะถูกแปลงเป็น `0` + 9 ตัวที่เหลือ
- ตัวอย่าง: `9930000036677` → `0993000003667`

### Date Format
- วันที่จะถูกแปลงเป็นรูปแบบ `dd/mm/yyyy`
- รองรับรูปแบบ: `DD-MMM-YYYY` (เช่น `03-NOV-2025`)

---

## 🆕 การเพิ่มบริษัทใหม่

หากต้องการเพิ่มบริษัทใหม่ ให้ทำดังนี้:

1. สร้าง Class ใหม่ที่สืบทอดจาก `BaseInvoiceExtractor`
2. เพิ่มใน `InvoiceExtractorManager.extractors` (เรียงลำดับความเฉพาะเจาะจง)
3. กำหนด `COMPANY_IDENTIFIERS` สำหรับตรวจสอบ
4. Implement methods:
   - `is_company_document()` - ตรวจสอบว่าเป็นเอกสารของบริษัทนี้หรือไม่
   - `extract_all_data()` - ดึงข้อมูลทั้งหมด

---

## 📊 สรุปข้อมูลที่ส่งออกไปยัง Excel

### ชีต "มีภาษีมูลค่าเพิ่ม" และ "ไม่มีภาษีมูลค่าเพิ่ม"
- ลำดับ
- ชื่อบริษัท
- เลขประจำตัวผู้เสียภาษี
- วันที่
- เลขที่เอกสาร
- ชื่อบัญชี / โค้ดบัญชี
- เปอร์เซ็นต์หัก ณ ที่จ่าย
- ยอดก่อนภาษีมูลค่าเพิ่ม
- ยอดภาษีมูลค่าเพิ่ม
- ยอดหลังบวกภาษีมูลค่าเพิ่ม
- หมายเหตุ
- ชื่อไฟล์ใหม่
- ชื่อไฟล์เก่า

### ชีต "ที่อยู่แต่ละบริษัท"
- ลำดับ
- ชื่อบริษัท
- เลขประจำตัวผู้เสียภาษี
- ที่อยู่

---

*อัปเดตล่าสุด: 2025-11-29*

